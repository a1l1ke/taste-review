# AGENTS.md
- 설계·리뷰 우선이며 소스·테스트·빌드 설정을 직접 생성/수정하지 않고 방향과 단계별 가이드를 제시한다.
- 리뷰는 `파일:라인` 형식으로 위치·원인·개선 방향을 명시하고 수정은 사용자에게 맡기며 최소 개념 스니펫만 쓴다.
- Java 17·Spring Boot 4.1.1·Gradle 기준이며 `build.gradle` 의존성과 toolchain을 임의로 변경하지 않는다.
- 동작 설명은 `HELP.md` 공식 문서와 가이드를 우선 인용하고 Thymeleaf·Spring Web·REST 원칙을 따른다.
- Boot 4·Security·JPA·Docker 문법은 `spring-test` 스킬을 따르고 버전 확인은 Boot 4.1.1 문서로 한다.
- Security는 `permitAll`+로그인·Basic 해제·CSRF 유지·`PasswordEncoder(BCrypt)`만 쓰고 기본 사용자 생성을 막는다.
- JPA는 `ddl-auto=validate`·`open-in-view=false`·Flyway·H2(`local/test`)/PostgreSQL(`prod`)을 따른다.
- Docker는 멀티스테이지·JRE17·비root·`PORT`·`/actuator/health` 기준이며 `Dockerfile` 변경은 지침으로만 제시한다.
- 테스트 우선: `spring-test` 스킬로 SRS `REQ-*`마다 실패 테스트→구현→통과 TDD·Given-When-Then BDD를 안내한다.
- 공유 living docs·PRD·spec·보고서는 `docs` 스킬 요청시에만 다루고 채팅 답변·요약·계획은 채팅에 남긴다.
- Word·PDF·PPTX·XLSX는 각 `docx`·`pdf`·`pptx`·`xlsx` 스킬로만 읽고 만들고 편집하며 포맷 미지정 요청에는 적용하지 않는다.
- 새 작업은 `find-skills`와 `npx skills find`로 관련 스킬을 먼저 탐색하고 없으면 직접 수행 후 반복 작업은 `skill-creator`를 제안한다.
- `opencode.json`·`.opencode/`·에이전트·MCP·권한 설정 변경시에만 `customize-opencode` 스킬을 적용하고 앱 코드에는 적용하지 않는다.
- `morning`·`import-memory`는 명시적 요청시에만 실행하고 파일 조작은 Read/Glob/Grep 우선·Bash는 빌드·테스트에만 쓴다.
- 이 문서는 최대 16줄·줄당 128자 이내로 유지하고 제안은 관련 파일 확인과 실행 가능한 확인 절차를 함께 제시한다.
